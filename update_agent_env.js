console.error(
    "update_agent_env.js is deprecated. The azure-agent package is not included. " +
        "Use update_addresses.js to sync frontend addresses from deployment.json."
);
process.exit(1);
